# Copy this file to .env and adjust values as needed

# Logging configuration (uses forward slashes for cross-platform compatibility)
# Leave empty to auto-generate cross-platform config
LOG_CONFIG_FILE=resources/logging_config.ini

# Sound file path (uses forward slashes for cross-platform compatibility)
# Relative paths are automatically converted for your platform
SOUND_FILE=resources/sounds/alarm_sound.wav

# Example paths (all work cross-platform):
# LOG_CONFIG_FILE=resources/logging_config.ini    # Linux/Mac/Windows
# LOG_CONFIG_FILE=config\logging.ini              # Windows-style (auto-converted)
# LOG_CONFIG_FILE=                                # Auto-generate cross-platform config

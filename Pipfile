[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pyinstaller = ">=6.11.0"
python-dotenv = ">=1.1.0"

[dev-packages]
behave = ">=1.2.6"
pytest = ">=8.3.5"
pytest-mock = ">=3.14.0"
flake8 = ">=7.2.0"
pytest-cov = ">=6.0.0"
pygame = ">=2.1.3"

[requires]
python_version = "3.11"
